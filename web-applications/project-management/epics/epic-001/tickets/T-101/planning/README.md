# T-101 Planning: Initial Scaffolding

## Task Breakdown
1. [ ] Create `/web-applications/backend` using Nest CLI.
2. [ ] Create `/web-applications/frontend` using Vite CLI.
3. [ ] Configure `tsconfig.json` and basic `.eslintrc` in both.
4. [ ] Implement shared theme provider in Frontend using tokens from `style_guide.json`.
5. [ ] Create root `package.json` for managing both services.

## Verification Checklist
- [ ] `cd backend && npm run start` works.
- [ ] `cd frontend && npm run dev` works.
- [ ] Frontend theme matches style guide primary color `#027CEC`.
